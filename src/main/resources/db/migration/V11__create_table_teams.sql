CREATE TABLE teams (
                      team_id INT PRIMARY KEY,
                      name VARCHAR(50),
                      country VARCHAR(50),
                      city VARCHAR(50),
                      budget INT,
                      stadium VARCHAR(50)
);